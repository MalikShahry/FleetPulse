# FleetPulse
A cloud-capable ASP.NET Core system that ingests GPS updates from vehicles (simulated), publishes real-time location updates via SignalR, supports geofencing alerts, stores route history, and exposes admin dashboards for live monitoring and analytics.
